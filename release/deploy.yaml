parameters:
  name: deps
  type: object
  default: 
  - Integration


jobs:
- deployment: Deploy
  displayName: 'Deploy'
  dependsOn: ${{ parameters.deps }}
  condition: and(in(dependencies.Integration.result, 'Succeeded', 'Skipped')
  environment: QA
  strategy:                 
    runOnce: 
      deploy: 
        steps:
        - bash: |
            echo "Deploy dev"