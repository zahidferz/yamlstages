jobs:
- deployment: Deploy
  displayName: 'Deploy'
  dependsOn:
  - Integration
  - Migration
  environment: QA
  strategy:                 
    runOnce: 
      deploy: 
        steps:
        - bash: |
            echo "Deploy dev"