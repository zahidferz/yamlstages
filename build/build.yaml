parameters:
  buildname: 'api'

jobs:
- job: "Build${{ variables.buildname }}"
  displayName: Build ${{ variables.buildname }}
  steps:
    - bash: echo "step 1" 
      displayName: step1
    - bash: echo "step 2" 
      displayName: step2